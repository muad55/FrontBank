import React from 'react'


//Le loader est une fonction qui s'exécute avant que la route ne soit rendue. Il récupère les données nécessaires (par exemple depuis une API ou une base de données).
export const loader = () => {}

const Landing = () => {
  return <div className="text-4xl">Landing</div>
}

export default Landing
